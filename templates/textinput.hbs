<div class="component__inner fileinput__inner">

  {{> component this}}

  {{! complex unless and if combination to set the correct classes for CSS to use in showing marking and disabled states}}
  <div class="component__widget fileinput__widget{{#unless _isEnabled}} is-disabled{{#if _isInteractionComplete}} is-complete is-submitted show-user-answer{{#if _isCorrect}} is-correct{{/if}}{{/if}}{{/unless}}">

    {{#each _items}}
    <div class="fileinput-item js-fileinput-item{{#unless ../_isEnabled}}{{#if ../_canShowMarking}}{{#if _isCorrect}} is-correct{{else}} is-incorrect{{/if}}{{/if}}{{/unless}}">

      {{#if prefix}}
      <div class="fileinput-item__prefix-container">
      <label class="fileinput-item__prefix" id="{{../_id}}-{{@index}}-aria" for="{{../_id}}-{{@index}}" aria-label="{{{prefix}}}">
        {{{prefix}}}
      </label>
      </div>
      {{/if}}

      {{#if ../_isEnabled}}
      <div class="fileinput-item__filebox-container">
        <input class="fileinput-item__filebox js-fileinput-filebox" type="file" placeholder="{{{placeholder}}}" data-id="input-{{@index}}" id="{{../_id}}-{{@index}}" {{#if prefix}}aria-labelledby="{{../_id}}-{{@index}}-aria"{{else}}aria-label="{{{placeholder}}}"{{/if}} value="true">

        <div class="fileinput-item__state">
          <div class="fileinput-item__icon fileinput-item__correct-icon">
            <div class="icon"></div>
          </div>
          <div class="fileinput-item__icon fileinput-item__incorrect-icon">
            <div class="icon"></div>
          </div>
        </div>

      </div>
      {{else}}
      <div class="fileinput-item__filebox-container">
        <input class="fileinput-item__filebox js-fileinput-filebox" type="file" placeholder="{{{placeholder}}}" data-id="input-{{@index}}" value="{{userAnswer}}" disabled="true">

        <div class="fileinput-item__state">
          <div class="fileinput-item__icon fileinput-item__correct-icon">
            <div class="icon"></div>
          </div>
          <div class="fileinput-item__icon fileinput-item__incorrect-icon">
            <div class="icon"></div>
          </div>
        </div>

      </div>
      {{/if}}

      {{#if suffix}}
      <div class="fileinput-item__suffix-container">
      <label class="fileinput-item__suffix" id="{{../_id}}-{{@index}}-aria" for="{{../_id}}-{{@index}}" aria-label="{{{suffix}}}">
        {{{suffix}}}
      </label>
      </div>
      {{/if}}

    </div>
    {{/each}}

  </div>
  <div id="feedback"></div>
  <div id="result"></div>

  <div class="btn__container"></div>

</div>
